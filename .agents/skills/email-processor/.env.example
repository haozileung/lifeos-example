# IMAP邮箱配置示例
# 复制此文件为 .env 并填入你的实际配置
# ⚠️ 重要：不要将 .env 文件提交到版本控制系统！

# IMAP服务器地址
# 常见服务器：
# - Gmail: imap.gmail.com
# - Outlook: outlook.office365.com
# - QQ邮箱: imap.qq.com
# - 163邮箱: imap.163.com
IMAP_SERVER=imap.qq.com

# IMAP端口（通常为993）
IMAP_PORT=993

# 邮箱用户名（通常是完整的邮箱地址）
IMAP_USERNAME=your@email.com

# 邮箱密码或应用专用密码
# 注意：
# - Gmail: 使用应用专用密码 https://myaccount.google.com/apppasswords
# - QQ邮箱: 使用授权码，不是QQ密码
# - Outlook: 可能需要应用密码
IMAP_PASSWORD=your-app-password-here

# 使用说明：
#
# 1. 复制此文件：
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入你的实际配置
#
# 3. 加载环境变量：
#    source .env
#
# 4. 运行脚本：
#    python3 scripts/imap_client.py --limit 10
#
# 5. (可选) 添加到 ~/.bashrc 或 ~/.zshrc 自动加载：
#    echo 'source /path/to/.env' >> ~/.bashrc
#
# 安全提示：
# - ✅ 将 .env 添加到 .gitignore
# - ✅ 不要分享 .env 文件
# - ✅ 定期轮换密码
# - ❌ 不要在代码中硬编码密码
